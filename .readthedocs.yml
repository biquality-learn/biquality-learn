version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"

sphinx:
  configuration: docs/conf.py

formats: all

python:
  install:
    - requirements: docs/docs-requirements.txt
    - method: setuptools
      path: .